
#pragma once
#include <vector>
#include <cstdint>

bool Algo1(int32_t round_number, std::vector<bool>& self_choices, std::vector<bool>& enemy_choices);
bool Algo2(int32_t round_number, std::vector<bool>& self_choices, std::vector<bool>& enemy_choices);
bool Algo3(int32_t round_number, std::vector<bool>& self_choices, std::vector<bool>& enemy_choices);